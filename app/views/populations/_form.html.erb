<%= form_with(model: population, local: true) do |form| %>
  <% if population.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(population.errors.count, "error") %> prohibited this population from being saved:</h2>

      <ul>
      <% population.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year, id: :population_year %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :population_country %>
  </div>

  <div class="field">
    <%= form.label :continent %>
    <%= form.text_field :continent, id: :population_continent %>
  </div>

  <div class="field">
    <%= form.label :population %>
    <%= form.text_field :population, id: :population_population %>
  </div>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :population_rank %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
